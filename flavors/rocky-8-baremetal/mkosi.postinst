#!/usr/bin/env bash

set -e
set -x

ls -la /etc/resolv.conf || true
cat /etc/resolv.conf || true

# ---------------------------- install updates ---------------------------------
echo -e "\n\Dunno rocky...\n"
